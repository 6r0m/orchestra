# Workbench UX: what needs you first, said once, explained in place

**Status:** REVIEW REQUIRED
**Scope:** the Workbench page — [static/](../app/interfaces/workbench/static/) (its HTML, CSS and
JavaScript), the server's list of the page's files in [server.py](../app/interfaces/workbench/server.py),
the page's browser acceptance ([demo.py](../tools/demo.py), [demo_press.py](../tools/demo_press.py)), and
the documents that describe the page
**Stable documentation owner:** [structure.md](../docs/architecture/structure.md) (architecture D29);
[docs/using.md](../docs/using.md) (the stack, the page); the interfaces'
[structure.md](../app/interfaces/docs/architecture/structure.md) (the page's composition)

## Contents

- [Goal](#goal) · [Authority register](#authority-register) · [Non-goals](#non-goals)
- [Verified evidence](#verified-evidence) · [Current architecture](#current-architecture-and-source-of-truth)
- [Problem](#problem) · [Decision](#decision) · [Invariants](#required-invariants)
- [Tasks](#implementation-tasks) · [Verification](#test-first-and-verification-plan)
- [Documentation](#documentation-plan) · [Rollout](#rollout-and-rollback) · [Completion](#completion-criteria)
- [Review record](#review-record)

## Goal

The operator sees at once what waits for them and why, and answers it with the evidence and each
answer's consequence in front of them. They watch the agent that is working, and read every fact once,
in plain words. The page stays a thin view over the API it uses today.

## Authority register

In this todo `D<n>` alone is this register's own entry; a decision of
[structure.md](../docs/architecture/structure.md) is written *architecture D<n>*.

### Operator decisions

- **D1** Fix the Workbench's design, UI and UX: more informative, without duplicates, with the right
  priorities and focus, obvious and very intuitive to use, with hints.
  - Effect: the page's layout, hierarchy, copy, hints and visual design are this change; the Goal states it.
  - Reason: not stated
  - Date/source: 2026-09-25, operator: *"fix design ui ux, it's should be more informative without
    duplicates, with right priority focuses, user obvious and very intuitive with hints"*
- **D2** Two web design skills are used: one to create the design, one to verify it.
  - Effect: `frontend-design` makes the design plan and builds it, with its self-critique on screenshots;
    `web-design-review` checks every changed page file against its pinned checklist.
  - Reason: not stated
  - Date/source: 2026-09-25, operator: *"you have 2 web design skills to create and verify"*
- **D3** Investigate first; implement only after the external reviewer's PASS and the operator's
  explicit GO.
  - Effect: this todo stays `REVIEW REQUIRED` until both.
  - Reason: not stated
  - Date/source: 2026-09-25, operator: *"/investigate-change first and wait reviewer pass and my explicit go"*
- **D4** If the web architecture needs improving, say so — it is better done now, in this change.
  - Effect: the page's code structure is in scope where evidence shows a need;
    [Web architecture](#web-architecture) says what and why.
  - Reason: not stated
  - Date/source: 2026-09-25, operator: *"if need better web arhcitecture say and better do it now"*
- **D5** This comes before the operator's manual check of configurable flows.
  - Effect: the live `architect-research` run of
    [the flows change](2026-09-25_1458-configurable-flows.md) happens on the redesigned page.
  - Reason: not stated
  - Date/source: 2026-09-25, operator: *"before I will do manual recheck - I need you to fix design ui ux"*

### Operator gates

- **Q1 [OPEN - NON-BLOCKING]:** may a role's terminal be collapsed to its one-line header while that
  role is idle — the working role's terminal open, either one a click away? Architecture D29 lists
  both roles' live terminals among what a run shows. Default while open: A6.
- **Q2 [OPEN - NON-BLOCKING]:** may the browser's `confirm()` be replaced by the page's own dialog,
  asking the same questions with a named confirm button and Cancel as the default? Default while open: A7.
- **Q3 [OPEN - NON-BLOCKING]:** should the page-capture instrument — fixtures from the real workflow
  and headless Edge, see [Verified evidence](#verified-evidence) — be committed under `tools/` for
  future page work, or stay a throwaway? Default while open: A10.

### Working assumptions

- **A1 [ACTIVE]:** no change to the server's JSON API, the workflow, `client.py` or `stack.py`. Every
  datum the design shows is already in the API; the inventory is under Verified evidence.
- **A2 [ACTIVE]:** system font stacks, with no vendored web font. The CSP is `default-src 'self'`, and
  the page's one vendored dependency, xterm.js, is already the pinned-by-hand cost the interfaces'
  structure names.
- **A3 [ACTIVE]:** *Force terminate* stays offered in every open state, placed after *Stop run* and
  styled as the break-glass action it is. Its explanation stays in its confirmation.
- **A4 [ACTIVE]:** the Start form moves into the main area. It shows when no run is selected, and from
  *New run* in the top bar, so the rail holds only the runs.
- **A5 [ACTIVE]:** the stack's reading stays at the top, one chip per part. Its Start, Stop and Restart
  — per part and for the whole stack — move into a menu opened from the chips. A part down, or one
  whose state cannot be read, raises a banner with what it blocks and its Start. Every stack action of
  architecture D32 stays one click from anywhere.
- **A6 [ACTIVE]:** the working role's terminal is open and an idle role's is collapsed; either opens
  with a click, and stays as the operator left it across refreshes (Q1).
- **A7 [ACTIVE]:** confirmations are the page's own `<dialog>`, with the same questions, a confirm
  button named for the action, and Cancel focused for a dangerous one (Q2).
- **A8 [ACTIVE]:** the selected run and view are in the URL's fragment (`#run=<id>`, `#new`,
  `#worktrees`). The browser keeps it, so a reload keeps the run and a row can be opened in a new tab;
  the page still holds no run state (architecture D29).
- **A9 [ACTIVE]:** answers keep their names — Approve, Revise, Guide, Continue, Merge, Discard, Stop run,
  Force terminate — because the command line and the docs use them, and the demo presses them by name.
  What each one does is said beside it.
- **A10 [ACTIVE]:** the capture instrument stays a throwaway; `make demo` stays the page's committed
  acceptance (Q3).
- **A11 [ACTIVE]:** the design's subject is a console for conducting an ensemble of agents. Its audience
  is one developer-operator at a Windows desktop in long sessions, beside dark terminals. Its primary
  job is to answer what waits with confidence, see what moves, and catch what is broken.
- **A12 [ACTIVE]:** dark only, and desktop first: full layout from 1280 px wide, usable at 900. At
  phone width it stays readable with no sideways page scroll; the terminals scroll inside themselves.

## Non-goals

- No change to what the workflow publishes, to what an answer means, or to which answers a stop offers.
  No change to the command line.
- No new endpoint or API field, no server-driven UI, no framework, no build step, no web font, no new
  vendored dependency.
- No light theme, no browser notifications, no push channel in place of polling, no new operator
  capability.
- No change to the flow files, such as a description field.

## Verified evidence

Captured by a throwaway instrument: headless Edge over the DevTools protocol, rendering the real page.
Every API answer came from fixtures that the real workflow produced on Temporal's time-skipping test
server with the suite's fake agents — a research brief revised then approved, a plan sent back once,
a blocker, a failed step, a spent round budget, the final gate, a merge — plus the at-work, stopping,
blocked and kept views built from those statuses. Nothing reached the server's API. Twelve states were
captured at 1600×1000, full page. The live Workbench, which held no runs, gave the empty state.

### Verified facts

**Defects**

1. **A closed run offers *Stop run* and *Force terminate*.** `.actions { display: flex }` in
   [style.css](../app/interfaces/workbench/static/style.css) outranks the `hidden` attribute:
   `#run-controls` measured `hidden = true`, `display: flex`, with both buttons visible on a stopped
   run. `#run-blocked` measured the same. The server refuses both presses.
2. **The research approval is titled "Approve the plan"**. `TITLES.approval` in
   [app.js](../app/interfaces/workbench/static/app.js) is one title for every approval, while the
   workflow's hint beside it says "approve to go on to the plan, or revise with feedback for new research".
3. **Before any plan exists, the page shows `todo: <path>` of the plan file.** `todo_path` is set when
   the worktree is made ([activities.py](../app/application/activities.py) `create_worktree`), and every
   stop publishes it ([workflow.py](../app/orchestration/workflow.py) `_stop`), including the research
   approval and a failed first plan.
4. **"blocked:" is glued to the elapsed time** in a run's row ("for 21:21blocked: the Windows worker is
   down"). `refreshRuns` appends the span with no separator.
5. **A failed run's log path overflows the rail.** Its row has no word breaking.
6. **Text cannot be selected reliably.** `refreshRun` rebuilds the whole history and the workflow lines
   every 2.5 s (`renderTimeline` calls `replaceChildren()`, and `lines.textContent` is reassigned), and
   `refreshRuns` rebuilds every row every 5 s. Also an inference: a text selection in them is cleared on
   the next refresh.

**Duplicates** (one fact, shown more than once)

7. A research brief is shown whole twice: in the stop card, as `stop.feedback`, and in the history, as
   `entry.brief` (workflow `_approval`, `_stage`).
8. The architect's feedback at an approval, a blocker or a spent budget is shown whole twice, in the
   stop card and in the history's last entry.
9. A failure is shown twice: its first line in the run's status line, and whole in the stop card.
10. "Waiting for you" is said three times — the row, the run's status line and the stop card's title.
    The elapsed time is shown three times.
11. The worktree path is repeated as the prefix of the plan path.

**Priority** (measured on the captures)

12. The stack panel is the first thing on the page: nine buttons, always visible while every part is
    up — *Start stack* among them, which then has nothing to start.
13. The Start form fills the top of the rail. "Needs you" begins 711 px down the empty page.
14. In a waiting run, *Stop run* and *Force terminate* come before the stop's answers.
15. Each terminal is about 715 px tall and shown whether live or idle. At the final gate the
    architect's verification and the change start about 2,270 and 2,360 px down, below both idle
    terminals.
16. The worktree tool, a maintenance view, sits in the rail at all times, with a second copy of the
    repository picker.

**Intuitiveness and hints**

17. Raw codes reach the operator: `approval`, `blocker`, `exhausted`, `final`, `MERGED`, `STOPPED`,
    "episode", "round 0", "phase research".
18. "for 12:28" can read as a time of day. It is minutes and seconds (`elapsed()`).
19. Four things are never explained on the page:
    - what *skip approvals* skips;
    - which of Repository and "or its path" wins — the path, silently;
    - which answers read "Your note";
    - how Stop run differs from Force terminate, until its confirmation.
20. A row shows about 45 characters of the task and nothing else that tells two runs apart, such as
    a start time or an id. With nothing selected, the main area is empty.
21. A refused answer ("revise needs the feedback to act on") shows below the buttons, not at the note,
    and focus stays where it was.

**Checklist baseline** — `web-design-review` on the three page files, 25 findings:
- no `color-scheme: dark`, so the checkbox and scrollbars render light;
- no skip link, no `aria-live` on the six result lines, no hover or keyboard access on the clickable
  `<li>` rows;
- hard-coded date and duration formats, no `tabular-nums` on the ticking times;
- unspecific labels ("Start", "Show", "Continue");
- no `name`, `autocomplete` or `spellcheck` on the fields, and placeholders that are not examples;
- the selected run is not in the URL;
- a smooth scroll that ignores reduced motion;
- no `translate="no"` on ids and paths.

**Consumers and constraints**

22. `tools/demo_press.py` reaches into the page in four ways:
    - the globals `select()` and `terminals[role]`;
    - the ids `start-task`, `start-repo`, `stop-note` and `start-result`;
    - the rule that a button's `-result` line is in its own `.card`;
    - `window.confirm`, whose question it records.

    `tools/demo.py` presses buttons by their labels. It checks the page's result words (`answered:`,
    `stopping`, `terminated`, `done: start WSL worker`, `not accepted`, `removed`, `started`) and the
    confirmations' words. [acceptance_restart.py](../tests/acceptance_restart.py) uses the API only.
23. [test_workbench.py](../tests/interfaces/test_workbench.py) proves the API and that the page's
    files are served; the page in a browser is `make demo`'s.
24. The server serves only the paths in its `STATIC` map.
25. The CSP `default-src 'self'` allows same-origin module scripts.
26. A terminal is fixed at 160×48, the size the host's PTY is made with (`terminal.py` `COLS, ROWS`), and
    the page's xterm must match it. A smaller xterm would garble a full-screen agent CLI, so an idle
    terminal can be collapsed or placed lower, never shrunk.
27. `review_diff` stages new files into a private index ([worktrees.py](../app/workspace/worktrees.py)),
    so at a plan's approval a run's change *is* its plan file. The existing change reader can show the plan.
28. **API inventory:** every datum the design shows is already served.
    - Each row: the run's view (`client.view`) — its state, stage, role, since, stop reason, failure,
      blocked_by, repo, host, flow, step and started time.
    - A run: the raw stop — id, reason, phase, todo, feedback, hint, actions and since.
    - The history: timeline entries — stage, phase, round, verdict, gate, feedback, brief and time.
    - The rest: the workflow's state (worktree, target), the run's links, the change in parts, the
      stack's reading, flows, repositories and worktrees.
    - A stop's phase is what tells a research approval from a plan's.

### Inferences

- Polling itself is sound for one operator. The damage in fact 6 comes from rebuilding what did not
  change. The code's own pattern — `shownRepos`, `shownFlows`, `shownStop` — already skips unchanged
  renders in several places.

### Assumptions and unverified areas

- An xterm opened inside a collapsed container may measure its characters wrong unless the container
  stays laid out. Task 9 verifies this on the page.
- `Intl.DurationFormat` in the operator's Edge is unverified. Where it is absent, the durations fall
  back to `Intl.NumberFormat` units.

### Refuted

- *The redesign needs new API fields.* Refuted by fact 28.
- *Terminals can be made smaller to fit.* Refuted by fact 26.

## Current architecture and source of truth

- **The page** is architecture D29's: plain HTML, CSS and JavaScript over the server's JSON API. It
  holds no state. Every write goes through `client.py` or the stack's owner (D32), and agent text
  reaches it only as text or terminal bytes.
- **Who owns what it shows:**
  - A run's facts are the workflow's status query, shaped by `client.view`.
  - The stack's reading is `stack.status`.
  - A change is `worktrees.review_diff`, read on the run's host.
  - The answers a stop takes, and whether one is accepted, are the workflow's (architecture D6). The
    page owns only their labels, colours and dialogs — the interfaces'
    [invariants](../app/interfaces/docs/architecture/structure.md#invariants) — and keeps no list of
    its own.
- **The page's code** is one classic script, [app.js](../app/interfaces/workbench/static/app.js) at
  765 lines, with 12 module-level mutable variables across its concerns. It polls three reads (stack
  5 s, runs 5 s, the selected run 2.5 s) and redraws each read's views.
- **Its acceptance** is `make demo`: real runs, the real Workbench and live terminals, every control
  pressed in headless Edge.

## Problem

The page shows everything D29 asks, but not in the order the operator needs it. Facts 12–16 rank the
least-used controls above the decision. Facts 7–11 say one fact in several places, and facts 17–21
name things by the system's words, not the operator's. Six defects (facts 1–6) make it wrong in
places, one of them on the research approval the operator is about to check (facts 2–3). Its code
redraws unchanged views on every poll (fact 6), and the redesign would take one mixed-concern script
past 1,000 lines — an estimate, about 400 lines of new views.

## Decision

### Design plan (`frontend-design`, first pass)

**Subject, audience, job:** A11.

**Principles**

1. **Attention first.** The rail's first group is *Needs you*; a run's first block is what you must
   decide. Routine controls sit one step away. Problems come to you as banners.
2. **One voice per role.** Engineer, architect and you each have one colour, used everywhere that role
   appears: the flow, the rows, the terminal headers, the history. Colour says *who*; words say *what state*.
3. **Evidence beside the decision.** The brief, the verdict, the failure, the plan and the change sit in
   or directly under the decision they support.
4. **Said once.** Each fact has one place. The history collapses an entry the decision already shows, and
   marks it *shown above*.
5. **Explain in place.** Each field and answer says what it does, in the docs' words. A code gets a
   plain name.
6. **Quiet chrome.** Sections are flat, separated by space and rules. One raised element — the decision.
7. **The memorable element is the score.** The run's flow is drawn as a line of cues: done steps quiet,
   the current one ringed, your cues in amber. It is an ordered list, with `aria-current` on the step
   the run is at.

**Colour** (dark; every text colour checked for WCAG AA on both surface and raised)

| name | hex | use |
|---|---|---|
| ground | `#101419` | the page |
| surface | `#161b22` | the rail, sections |
| raised | `#1d232c` | the decision, menus, dialogs |
| rule | `#2a313b` | lines and borders |
| ink | `#e6e9ee` | text |
| quiet | `#9aa3b0` | secondary text (6.8:1 on surface) |
| engineer | `#3fb3c2` | the engineer's voice (7.0:1) |
| architect | `#a98bf5` | the architect's voice (6.3:1) |
| you | `#f0b44a` | your cues, *Needs you*, attention (9.3:1) |
| pass / bad | `#5cc08c` / `#ec6a6a` | verdicts, down, danger (7.7:1 / 5.6:1) |

**Type:** the platform's own UI face — `"Segoe UI Variable Text", "Segoe UI", system-ui, sans-serif` —
and `"Cascadia Mono", Consolas, ui-monospace, monospace` for ids, paths, the diff and the terminals (A2).
The scale is 12 / 13 / 14 (body) / 16 (sections) / 20 (the run's task), weights 400 and 600, sentence
case throughout, and tabular figures for times. An agent's prose — a brief, feedback — is set in the UI
face at up to 80 characters a line, with its line breaks kept; today it is set in monospace.

**Layout** (left-aligned)

No run selected: the New run form is the main area's content.

```
┌ Orchestra Workbench   ● Temporal  ● WSL worker  ● Windows worker ▾        Worktrees   [ New run ] ┐
├ banner, only when a part is down: "The Windows worker is down — 2 runs cannot move.  [Start it]"   ┤
├ rail ─────────────────────────┬ main ─────────────────────────────────────────────────────────────┤
│ Needs you  3                  │  Start a run                                                      │
│ ▌Approve the research brief   │  Task         [ what should be true when it is done…            ] │
│  Move the nightly export fr…  │               The first words name the run and its branch.        │
│  exporter             12 min  │  Repository   (•) from repos.json [ work/platform/exporter ▾ ]    │
│ ▌The architect raised a…      │               ( ) a path          [ /mnt/e/Repos/name…         ]  │
│ Working  2                    │  Flow         [ architect-research ▾ ]                            │
│ ▌engineer building    16 min  │               ◯ architect research ─ ◯ you approve ─ ◯ engineer … │
│ Finished                      │  [ ] Skip approvals                                               │
│  Merged              4 h ago  │      Skips each "you approve"; a blocker, a spent budget, a       │
│ [ Older runs ]                │      failed step and the merge still wait for you.                │
│                               │  [ Start run ]                                                    │
└───────────────────────────────┴───────────────────────────────────────────────────────────────────┘
```

A run waiting for you — the evidence inside the decision:

```
Move the nightly export from cron to the platform scheduler, one job per tenant     [ Stop run ] Force terminate
Repository work/platform/exporter   Host wsl   Run and branch move-the-nightly-a1b2c3d4 ⧉   Worktree /path/… ⧉
✓ architect research ─ ◉ you approve ─ ◯ engineer plan ─ ◯ architect assess ─ ◯ you approve ─ …
Waiting 12 min for you (since 14:03)
┌ Approve the research brief ──────────────────────────────────────────────────────────────────┐
│ approve to go on to the plan, or revise with feedback for new research   (the workflow's hint) │
│ The architect's brief                                                                          │
│   Brief: the nightly export runs from cron on one host, so a missed night is silent …          │
│ Your note, sent with Revise    [                                                            ]  │
│ [ Approve ]  [ Revise ]                                                                         │
└────────────────────────────────────────────────────────────────────────────────────────────────┘
▸ engineer   idle, recorded         ▸ architect   idle, recorded
History     research   round 1   brief ▸        research   round 2   brief, shown above
▸ Workflow log                                                          Temporal · Langfuse
```

At phone width the rail folds above the main area as a collapsible list, and the facts and the score
wrap.

**What the run shows first, by state**

| run | first | then |
|---|---|---|
| waiting at an approval, a blocker or a spent budget | the decision, with its evidence | the plan (at a plan's approval), terminals collapsed, history |
| at the final gate | the decision, with the architect's verification | the change, terminals collapsed, history |
| failed | the decision, with the failure and its log path | the failed role's terminal, history |
| working | the working role's terminal, open | the other terminal collapsed, history, the change once built |
| stopping | what it waits for, with Force terminate | terminals, history |
| closed | how it ended; what it kept, with Remove | the change if kept, history |

**Decisions** — titles and hints are the page's; the answers are the stop's own (A9)

| stop | title | evidence shown | its note is sent with |
|---|---|---|---|
| approval, phase `research` | Approve the research brief | the brief (`stop.feedback`) | Revise |
| approval, phase `plan` | Approve the plan | the architect's assessment, and the plan read from the change (fact 27) | Revise |
| approval, phase `build` | Approve the build | the architect's verification, and the change | Revise |
| blocker | The architect raised a blocker | the blocker | Guide |
| exhausted | The review rounds are used up | the last finding | Guide |
| failed | A step failed | the failure, with its log path to copy | — |
| final | Ready to merge | the last verification, the change, and a refused merge's reason | either Revise |

The workflow's hint stays each decision's subtitle, and is said once. The plan path is shown only from a
plan on (fact 3), relative to the worktree (fact 11). A refused answer is shown at the note, and focus
moves there (fact 21).

**Copy**
- Stop reasons and statuses become words: "Merged", "Stopped, worktree and branch kept".
- Times are durations — "12 min", "1 h 5 min" — through `Intl`, with the local clock time where it helps.
- Paths and ids carry `translate="no"` and a copy button.
- Empty states say what to do: "Nothing is waiting for you."
- The tab title counts what waits: "(2) Orchestra Workbench".

### Review against the brief (the design skill's check)

A generic plan for "redesign an operations dashboard" converges on six things. Each is changed here
because this brief is different:

- **Metric cards on top.** Dropped: the operator acts on no metric, and counts live in the group headers.
- **Green, yellow and red status badges everywhere.** Replaced by the role voices and one attention
  colour, so colour tells who is acting and whether it is you.
- **Identical cards for every section.** Replaced by one raised element, the decision; the rest is flat.
- **A generic stepper.** Replaced by the score, whose amber cues are the operator's own steps.
- **Metadata strings joined with dots.** Replaced by labelled facts, which explain themselves.
- **Walls of controls.** Replaced by a menu and banners.

### Web architecture

Evidence of need (D4): one 765-line script whose 12 shared mutable variables cross its concerns; views
redrawn on every poll (fact 6); two globals that tools reach into (fact 22); and ~400 more lines this
design adds. The change, with no framework, build step or dependency:

1. **Native ES modules by concern**, loaded as `<script type="module">`, with `app.js` as the entry that
   wires the polls and the URL route:
   - `api.js` — the token and requests;
   - `ui.js` — the text-only element helpers, formats and the dialog;
   - `stack.js`, `runs.js` (the rail), `start.js` (the form, the repository picker, the flows),
     `run.js` (header, score, decision, controls, kept), `history.js`, `change.js`, `terminals.js`,
     `worktrees.js`.

   Each module keeps its own state; xterm.js stays a classic script ahead of them.
2. **Redraw only what changed:** each view keeps the signature of what it last drew and skips an unchanged
   read — the pattern of `shownRepos` and `shownStop`, applied to every view. A selection, focus, an
   open section and scroll positions then survive a refresh. There is no virtual DOM.
3. **The URL fragment is the route** (A8). The rows become links (`<a href="#run=…">`), which gives
   keyboard, middle-click and reload.
4. **`[hidden] { display: none !important }`** so that no layout rule can show what the page hid (fact 1).
5. **The page's own `<dialog>`** for confirmations (A7), with the questions unchanged.
6. **The server's `STATIC` map lists each module.** It stays an explicit allowlist, now tested.

### Premise / KISS gate

- **Owner.** The page, a presentation layer, owns this outcome. The data already has its owners, and the
  design reads them unchanged (A1).
- **Adds.** Module files and one allowlist entry per file on the server; one dialog, in place of the
  browser's confirm; the fragment route.
- **Removes.** The redraw of unchanged views; the always-visible stack buttons, lifecycle wall and
  worktree tool; the duplicated texts; the second repository picker on screen.
- **Given up, knowingly.** Controls that are always on screen: the stack's, one click away now; an idle
  terminal, collapsed (Q1); the browser's native confirm (Q2).

### Alternatives considered

- **Restyle and fix in place, one script.** The fewest files. It fixes facts 1–5, but leaves fact 6's
  redraws and the growth past 1,000 lines. D4 asks for the better structure when it is needed, and the
  evidence says it is.
- **A UI library (Preact, lit-html) or a bundler.** Rejected: a new vendored dependency or a build step,
  against architecture D29's plain page, for no need a module split does not meet.
- **Push (server-sent events) instead of polling.** Rejected: Temporal gives the page no event for a
  status change, so push would need a watcher per run on the server. The polls are cheap for one
  operator; the harm was the redraw.

## Required invariants

1. **Architecture D29 holds.** Every read and write goes through today's API; the page holds no run
   state. The fragment is the browser's, and names a run the server is still asked about.
2. **A stop's answers are the ones it publishes** (architecture D6):
   - the page never adds, hides or disables a published answer;
   - it never decides whether one is accepted — the workflow refuses, and the page shows the refusal;
   - labels, titles, hints and dialogs are the page's.
3. **Agent and run text reaches the DOM only as text**: `textContent` and text nodes, never markup from
   the API.
4. **Every capability stays reachable:**
   - each stack part's and the whole stack's start, stop and restart;
   - a start on a repository or a path, a flow and *skip approvals*;
   - every answer, *Stop run*, *Force terminate*, and the removal of what a run kept;
   - the worktrees view, older runs, and the change in parts with its snapshot check;
   - the terminals' typing and Esc, and the Temporal and Langfuse links.
5. **Every confirmation still asks before its action**, in words that keep the consequences
   architecture D31 and D32 state. `tools/demo.py` checks those words.
6. **The terminal stays 160×48**, the PTY's size (fact 26).
7. **Security as today:** the token in its header, never in a URL; the CSP unchanged; loopback only.
8. **Accessibility floor:**
   - every control reachable by keyboard, with a visible focus;
   - results announced through `aria-live`;
   - reduced motion honoured;
   - colour never the only signal.

## Implementation tasks

0. [x] **Baseline:** the twelve before-captures and the checklist baseline (Verified evidence).
1. [ ] **Red guards first:**
   - `make demo` checks that a closed run offers no *Stop run*. It fails today (fact 1).
   - `test_workbench` checks that every file `index.html` and the modules load is served, with its type.
     Its control: one entry left out of `STATIC`.
2. [ ] **Structure, behaviour unchanged:**
   - the modules, the entry and the `STATIC` entries;
   - `[hidden]`;
   - redraw only on change.

   The page is captured again in every state, and must say what it said before.
3. [ ] **Tokens and base elements:** colour, type, spacing; primary, secondary, danger and quiet buttons;
   fields; visible focus; `color-scheme: dark`; tabular figures; the skip link and landmarks.
4. [ ] **Top bar and stack:**
   - the chips, and the menu with each part's and the whole stack's actions — *Start stack* only while
     a part is down;
   - the banner for a part down or unreadable, naming what it blocks, with its Start;
   - results in `aria-live`.
5. [ ] **The rail:**
   - link rows with the decision's title or the work under way;
   - the task, clamped to two lines;
   - the repository and the time as a duration;
   - the blocked badge and `aria-current`;
   - counts on the groups, empty states, and older runs.
6. [ ] **New run:** its hints, repository or path as one choice, the flow's score, *Skip approvals*
   explained, *Start run*; then the new run opens.
7. [ ] **The run's header:**
   - the task and the labelled facts, with a copy button for the id and each path;
   - the score and the status sentence;
   - the blocked banner with its Start;
   - *Stop run*, and *Force terminate* after it (A3), none while closed;
   - what a closed run kept, with *Remove*;
   - the links.
8. [ ] **The decision:**
   - its title by reason and phase, the workflow's hint, and its evidence;
   - the note, labelled with the answers it is sent with;
   - the answers, in the stop's order;
   - a refusal shown at the note;
   - the change read, and placed below, at a plan's approval and at the final gate.
9. [ ] **Terminals:**
   - role-coloured headers with each one's state and hint;
   - the working role's open and the idle one collapsed (A6) — kept laid out, so xterm measures right;
   - each left open or closed as the operator set it, across refreshes.
10. [ ] **History:**
    - by phase, each entry with its role, action, round, verdict and local time;
    - its text collapsed, and marked *shown above* when the decision shows it;
    - what the verdicts mean.
11. [ ] **Change:** its file summary; its patch with added, removed and hunk lines marked; a new file
    readable as text; the next part's size through `Intl`.
12. [ ] **Worktrees view:** its repository choice; a table of branch, state (with what it means), run,
    path and *Remove*.
13. [ ] **Dialogs** (A7) with the existing questions; the tab title's count; empty states; `translate="no"`.
14. [ ] **Consumers:**
    - `demo_press.py` — the route in place of `select()`, the terminal read from its screen, the dialog in
      place of `confirm`, and the absence check of task 1;
    - `demo.py` — any label or word that changed, and the new check.
15. [ ] **Docs** (below).
16. [ ] **Design critique:** `frontend-design`'s self-critique on the after-captures — every state, at
    1600, 1280, 900 and 390 px wide. Remove what does not serve the brief.
17. [ ] **Verification** (below), then the one agent review round, the external review and the full
    suite, in the operator's order.

## Test-first and verification plan

### Red evidence

| case | kind | today |
|---|---|---|
| a closed run offers no Stop run or Force terminate, in `make demo` | permanent guard | fails: both shown (fact 1) |
| every file the page loads is served with its type | permanent guard | passes today; red by its control, one entry left out |
| the page in twelve states — each fact once, the order of the state table, the decision titles, no raw codes, no overflow | acceptance, reviewer-checked on captures | facts 2–5, 7–21 |
| `web-design-review` on each changed page file | acceptance | the 25-finding baseline |
| a selection in the history survives a refresh | acceptance, probed in the capture instrument | cleared every 2.5 s (fact 6, inference) |
| every control of `make demo` pressed through the redesigned page | acceptance, the real route | passes on the old page; must pass on the new |

### Green evidence

- The two guards green, each shown red by its own fault first.
- `make demo`, whole, on the redesigned page. It runs on the live Temporal (`make up`) with a stack of
  its own.
- The after-captures of every state at four widths, beside the before-captures, reviewed against the
  state table and the decision table.
- `web-design-review` on the changed files: no finding left, or each one kept for a reason stated.
- `test_workbench` on WSL.
- `git diff --check` and `make public-check`.
- The operator's standing order:
  1. one agent review round, whose findings are fixed without further agents;
  2. the external reviewer's PASS;
  3. the whole suite once per host;
  4. the operator's manual check — the flows' live `architect-research` run, on this page (D5).

## Documentation plan

- **[structure.md](../docs/architecture/structure.md), architecture D29:** its sentences about where the
  stack's controls sit, and which terminals are open (Q1), are edited in place. The plain page with no
  build step stays true.
- **[docs/using.md](../docs/using.md):** *The stack* and *The page* are rewritten for the top bar, the
  menu, the banners, New run, the decision and the worktrees view. The flows' paragraph keeps its facts.
- **Interfaces [structure.md](../app/interfaces/docs/architecture/structure.md):** the composition row
  for `workbench/static/` names the page's modules by concern.
- **[tests/README.md](../tests/README.md) and [tools/README.md](../tools/README.md):** the rows for
  `test_workbench`, `demo.py` and `demo_press.py`, where what they check or how they press changes.
- **Duplication avoided:** the page's copy describes behaviour that D6, D29, D31 and D32 own, as
  interface text; no document restates the page's labels.
- Stable docs, code, comments, tests and configuration do not reference this todo.

## Rollout and rollback

- **Rollout:** the page's files are read on every request, but the `STATIC` map is read at the server's
  start, so `make workbench-restart` loads the new page. The runs and the stack are untouched: the page
  holds no state.
- **Rollback:** revert the change and `make workbench-restart`.

## Completion criteria

- Tasks 1–17 done.
- Both guards green, each shown red first.
- `make demo` passes whole.
- Every state captured at four widths and reviewed.
- `web-design-review` clean or each finding answered.
- The docs updated.
- The agents' round fixed; the external reviewer's PASS; the whole suite once per host; the operator's
  manual check done on this page.

## Review record

### 2026-09-25 — opened

- **Trigger:** *"bro before I will do manual recheck - I need you to fix design ui ux, it's should be more
  informative without duplicates, with right priority focuses, user obvious and very intuitive with
  hints … you have 2 web design skills to create and verify … /investigate-change first and wait
  reviewer pass and my explicit go … if need better web arhcitecture say and better do it now"*.
- **Evidence gathered:**
  - the page's code, its server and the API's owners;
  - architecture D6, D29, D31 and D32, and the interfaces' invariants;
  - the demo's hooks into the page;
  - twelve states captured from fixtures the real workflow made, and the live empty state;
  - `web-design-review`'s baseline.
- **Authority:** D1–D5 added; Q1–Q3 opened; A1–A12 added.
