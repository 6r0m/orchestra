# Done

Plans whose change was merged. A run moves its todo here at the final gate, with a finished
status line; the file is kept so the reasoning behind a merged change stays reachable from the
change itself.

A repository may say it deletes a finished plan instead, by setting `todo_done_dir` to `null` in
its `repos.json` entry. This repository keeps them.
