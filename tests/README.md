# Orchestration tests

Run from anywhere on WSL:

```bash
bash run-tests.sh
```

It syncs this checkout's uv-managed environment (`app/foundation/envpath.py`), discovers every
`test_*.py`, at any depth, and runs each test class in a process of its own, a few at a time — a whole
run takes about as long as its slowest class. On Windows, the files that exercise a host run in that
host's own environment, and the list of them lives in the script:

```
powershell -NoProfile -ExecutionPolicy Bypass -File run-tests.ps1
```

While working on a change, name what it touches — modules, classes or single tests, as unittest names
them — and only those run: `bash run-tests.sh tests.interfaces.test_cli`. The whole suite, on each host
the change touches, runs once, when the change is otherwise done. A run fails on anything short of every
class passing whole: a failure, an error, a module that does not load, a class that ran fewer tests than
it holds, a run that found none, or a class still running after ten minutes — its threads dumped, then
its processes ended. However a run ends, interrupted included, it leaves no class running. One run per
host at a time.

The two hosts' suites share one localhost and its test ports, so run them one after the other.

## How the suite is laid out

One folder per package under [app/](../app/README.md), named after it, holding that concern's tests.
The shared harness — the fakes, the controls, the fixtures and the recorded histories — stays at this
root, because it belongs to no single concern.

| folder | covers |
|---|---|
| `foundation/` | [app/foundation](../app/foundation/README.md): the persona-file resolver and the flows, on either host |
| `orchestration/` | [app/orchestration](../app/orchestration/README.md): the workflow, its routes, its stops and replay |
| `application/` | [app/application](../app/application/README.md): the stack's one owner |
| `workspace/` | [app/workspace](../app/workspace/README.md): repository descriptors and the worktree lifecycle |
| `agents/` | [app/agents](../app/agents/README.md): containment, live terminals, turn completion, trust records |
| `observability/` | [app/observability](../app/observability/README.md): the trace and its parity with the contract |
| `interfaces/` | [app/interfaces](../app/interfaces/README.md): the workbench's API over real runs, and how the command line types a stop's answers |
| this root | `test_architecture.py`, the live acceptance, and the harness every folder shares |

The rest of `app/application` — the client of runs and the activities — and the rest of
`app/foundation` — policy validation and path derivation — have no tests of their own: both are proven
through the concerns that use them, and `test_architecture.py` owns what is true of the packages
themselves. Add a folder when a concern gains tests that are its own rather than its consumers'.

| file | proves |
|---|---|
| `orchestration/test_workflow.py` | the workflow's contract on Temporal's time-skipping test server, over the fake seams: a run following its flow — research first and read-only, its brief shown at the approval with no verdict read and no tree judged, research again on a revise, the plan's prompt carrying the brief but not the words the research was revised with, then assessment, build, verification and the merge — controls: review taken from access, the brief not carried, the words carried on, research run writable; a research session born again given the brief its feedback is about — control: the brief left out; research alone ending `DONE` once approved, closing its terminals — control: `DONE` left out of the close; a flow with no build ending `DONE`, its worktree kept, nothing merged — control: the run ending at the final gate; an approval after a verify saying where approving goes, and a blocker in a flow's last part keeping its own hint — controls: the approval's text at every last stop, the plan's hint after any work; *skip approvals* skipping every approval but never a blocker, a spent round budget or the final gate — controls: a blocker skipped too, the budget skipped too; a flow the run is handed taken as given — a dict of steps whose keys would hold, a string, no steps, a name that is not a flow's, or steps that break a rule each ending it `REFUSED` before any activity — controls: the check taken out, the steps made into a list; a run started through the client keeping its flow when the flow's file changes — control: the file read again at each step; a run started before flows following the order those runs took; a brief read from the final message, and an empty one no answer — control: an empty brief taken as the raw events; verdict routing over `PASS/PATCH/UNVERIFIED/BLOCKER`; the exact `max_rounds` boundary; malformed reviewer output and a broken stream stopping as failures, never as verdicts; a resumed stop re-running no agent; guidance re-entry resetting the round; session identity with exact-id resume and `--last` banned; the architect launched read-only; mid-loop session loss rehydrating completely for both roles; guidance reaching the architect; log names unique across episodes; policy validation — a workflow queue of its own required, a plain token; a default flow named by the flows' own grammar — control: the generic token taken instead; a Stop's cleanup bound optional, from 1 to 60 seconds — it may only shorten the minute — the D3 independent-judge rule and one queue per target host; Continue running only the failed stage; no stop offering `abort`, and one sent anyway refused; and an answer from another process continuing the run while an unknown run id is refused. Every workflow scenario fails against `empty_workflow.py` |
| `orchestration/test_stops.py` | answers a stop does not offer are rejected before they reach history, and one answer sent twice is applied once; the final gate publishes a revise per role, a revise named that way reaching its role through the shared client, and the role rule read from what a stop published — control: `control_workflows.NoValidatorRun`; a lost role activity launched once — control: `RetryControl` under Temporal's default retries; a role running only on its target host's queue — control: the queue, not the target, decides; a refused repository creating no worktree; the final gate — merge, revise to the engineer or the architect, a discard that needs confirming, a conflict handed back to the engineer, a refused merge staying at the gate; a failed stage taking `continue` alone; and a Stop from each open state — an agent working, which ends with it, a stop waiting, a failed stage, the final gate, a host with no worker, whose cleanup waits its policy's bound, not a minute — ending the run stopped with no git run, a merge already running landing first and deciding the end, and the command line's `--stop` — which, with no worker to read the run, records the Stop at once and says so, as an answer is refused at once, naming the worker (control: the run's status read first); force terminate from the command line, and a terminated run's working agent ending at its next heartbeat — control: at work, it does not end by itself — while a merge already running goes on to its end after the run has closed — control: nothing had merged when it closed — and what the terminated run kept is not removed under it, the removal refused on its host as busy until the merge has landed, then run once; a removal no worker of its host takes refused once its bound is out, naming that host, and never run, not even when a worker comes back — controls: the removal unbounded, and the refusal not naming the host; a Stop while a merge waits on a queue whose worker is gone ending the run stopped once the merge has waited a heartbeat interval, git never running it, not even once a worker is back — control: without that bound it waits for good; without a Stop, that merge stopping for the operator, saying no worker took it, and Continue merging once a worker is back; and a Stop while a stop's trace row is written ending the run stopped — control: a trace write that swallows the Stop |
| `observability/test_observability.py` | what the operator observes: a run that begins with research writing its phase and its brief as the step's response, and the architect's reasoning on its steps alone — control: the engineer's asked too; console lines, `--show`, the preflight refusal, an injected client resolving no Langfuse client, a start whose run id meets a retained run drawing a fresh id, the Makefile passing task text verbatim; and the trace — the final diff equal to `gdiff -s`, the key pair read from `.env`, only a traced Claude role-run switching its plugin on with private settings removed on every way out, one work item whose phases open once and whose rounds keep counting across guidance, stops and answers where they belong, the Codex upload, the trace link, and the trace contract's names, levels, error types, scores, tags, environment, release, masking and dashboard; a worker counted as polling only while its last poll is recent — control: any poller Temporal still lists; a role step cut short from outside — by a Stop, whether its cancellation or its cleanup ending the agent reached the step first, closed at DEFAULT saying the run was stopped, with no error type; by a force terminate, a heartbeat timeout or its worker stopping under it, ERROR, `lost`; each signal read on its own as well — the control: a turn that fails on its own is still ERROR |
| `observability/test_stale_settings.py` | with real processes, on either host: a stage killed while it runs leaves its settings, key and all — the control — and the worker's own entry point, started after it as a restart starts it under a policy of its own, removes them; a stage still running keeps its own; the sweep after a stop takes the settings of the pid it names gone even while another process holds that pid — control: unnamed, a live pid's settings stay; and a sweep that cannot remove a dead stage's settings — a file held open, a folder not writable — names them and fails, so no stop is reported done over them |
| `observability/test_trace_parity.py` | the Temporal run writes, row by row, the rows kept for the same scenario in `fixtures/trace_rows.json`; repository and target from the run; no trace write ever retried; replaying a run writes no row — control: `TelemetryInWorkflow` writes one again |
| `orchestration/test_replay.py` | the recorded histories in `histories/` replay on the workflow — two of runs that follow a flow, one beginning with research and one ending `DONE`, and every one of them whatever `flows/` says, for the workflow never reads a flow — control: the order of a run started before flows read from today's file; among them two runs ended by `abort`, which no stop offers now, so the runs that took one still replay, and one run under the git step's wait for its worker, whose merge no worker took and Continue then merged — control: `ChangedRun`, one unpatched change, fails |
| `application/test_stack.py` | the stack's one owner: Temporal started before the workers and stopped after them, and never started again by Docker at WSL's start — control: `unless-stopped`, a worker not started while Temporal does not answer, a restart as a stop then a start, one action at a time, and another policy's stack never touching Temporal or the Windows worker; a worker counted stopped only once its process is proven gone, and only then what its stages left swept — control: a stop that cannot prove it sweeps nothing; a start proven only by the process its pid file names polling every one of its queues since it began — control: a dead worker's polls and an unpolled queue prove nothing; the reading's up, starting and down, Temporal out of reach included; a worker whose process cannot be read never counted stopped or down — unknown, saying why; a Windows worker this side would start elevated shown as one it cannot start, and left running by a restart; Temporal proven up only once it answers and stopped only once it does not; a stop whose sweep failed reported so, never as done; a connection kept across Temporal's stop reading it down and saying why; every stack action on a lock of the test's own, never the live stack's; a policy of the test's own never given one port twice, the one it holds included, though the system hands a port back — control: a pick blind to the policy's others; and, over real processes, a stand-in worker's stop through `workers.sh` taking its stage's settings with it at once, `workers.ps1` knowing the Windows worker by its command line and sweeping once it is gone, a host's script that never returns given up on at its action's limit, a part not done, and the stack's lock let go — control: the call unbounded; the pid the owner proved gone reaching the sweep through each host's own script, so a stage's settings under it are taken though the pid now runs another process — controls: `workers.sh`, `workers.ps1` or the worker dropping it; a stop whose sweep cannot take what its stages left reported so through `workers.sh`, and on Windows `workers.ps1`'s sweep failing over a settings file held open and naming it — control: the scripts swallowing the sweep's failure; a pid file naming another process never taken for the worker, and a second worker of one policy stopping at the port the first holds, leaving its record as it was — control: the record written first — on either host |
| `agents/test_launch.py` | a user systemd manager that hangs neither hangs a refused launch nor keeps a tree alive after its owner's death — control: the hanging shims do hang; a role-run's whole process tree ending on a timeout, a Stop, the agent's exit and its owner's death, on both hosts, and on Windows gone the moment its end returns, the folder it worked in removable at once — controls: no wait, and the job looked at only after its termination, which drops every process from its list; no process joining the tree once its end has begun — control: the job left open to newcomers; and an end it cannot prove — the job not closed, a process it still lists that cannot be opened, its termination refused, a wait failed or spent — raising rather than returning — controls: the termination unchecked, the unopenable process skipped — while one gone between the look and its opening needs no wait — control: every failed opening raising — every case run twice, the second time against a descendant that starts a session of its own to escape; the owner killed at moments spread across the launch itself — control: an uncontained launch leaves survivors; on Windows the owner killed after creating the agent and before it could be placed in a job leaving nothing — the agent is born inside its job; on POSIX a launch with no reachable systemd user manager refused before the agent runs — control: the same agent runs when it is reachable; task text reaching argv and stdin byte for byte — control: a shell executes it |
| `workspace/test_worktrees.py` | against real repositories: a run id that reads as its task and is a valid branch, folder and short enough for a Windows path; a stale branch of the run's name refused while one still at the base is adopted; a Windows worktree past the path limit refused before it exists — control: one character shorter fits; a worktree created in its root and adopted when made again; LFS files kept as pointers; the controller-only guard; an agent's git unable to reach a remote, and a push it actually attempts — including through `-c protocol.allow=always` — reaching none, control: the same push outside that environment lands; the worktree view; the approved merge, its plan moved or — for a repository without a done folder — deleted, and cleanup; a conflict handed back and merged after its resolution; a merge whose commit was refused after the plan was finished merging when continued, with or without a done folder and with the plan's line endings kept, and one refused after its work commit was written merging when continued; a merge already written adopted rather than repeated, while another run's merge carrying the same message is not; cleanup refusing an unmerged branch and removing nothing; discard; and every deletion guard on a worktree's environment |
| `agents/test_terminal.py` | a role turn in a live terminal, through the real PTY host, hooks and contained tree against `fake_cli.py` on both hosts: it ends on its own turn's completion only — a prompt of another session, an earlier prompt, a `Stop` while a background task runs or Codex's title turn never end it, while the completion of a redirect the operator typed after Esc does, and a resumed Codex thread's cumulative inputs match, control: events a first-event rule would accept; a multi-line prompt reaching the agent whole; the Codex thread taken from its turn and resumed by; `StopFailure`, an exit without completion and a timeout failing the step and ending the agent; the next turn replacing the agent and appending to the record; Esc and typing reaching the agent while an interrupt completes nothing; the run's close and the worker's death ending the agent and a detached descendant — control: a descendant outside the terminal survives; the WebSocket refusing another origin, a missing token and an unknown role, sending a live terminal's record then its bytes and taking keystrokes, and replaying a closed one read-only; a Claude hook payload read as UTF-8 whatever the locale; a lost session showing each CLI's measured message for rehydration; a change to the worktree during the architect's verify turn failing the step, control: an untouched worktree verifies; closing a run closing its viewers' connections; the merge and the discard ending the run's agents before git runs — and stopping before git, every terminal of the run closed all the same, when one agent's end is not proved — control: the close stopping at the first failure; a step failed by its checks ending its agent; an agent never inheriting a parent Claude Code session |
| `interfaces/test_workbench.py` | the workbench's API over real runs on the time-skipping server: the flows the page lists with the default among them, a run started on the one chosen — its view saying its flow and the step it is at — and one that does not hold refused with its reason; a run naming none following the policy's default — control: the default ignored; each process's workbench on the port the system gives it at bind, several processes binding at once, each on its own — control: a fixed port, where they collide; the token, another origin and a rebound host name refused; the page carrying its config and forbidding framing; the one loop's call cancelling work that outlives its limit, rather than letting it finish unseen; the stack's reading — Temporal and each host's worker, each managed or not, and Temporal out of reach reported rather than failed, the workers' processes still read — and its controls reaching the stack's owner only — the whole stack as the page asks for it, naming no part, reaching the owner as every part (control: a missing part defaulted to one) — the Workbench itself and an unknown action refused, a second action while one runs refused as busy; a run blocked only by a down worker of a queue it needs — a Windows run by the Windows worker alone, a WSL run never by it — a run on another stack's queues not judged, and a run whose workflow worker is down still shown, blocked by it, after a status read bounded to a moment — control: the read unbounded — and a kept worktree whose run's status does not come in time listed as not removable, after the same bounded read — control: the same; what a stopped run kept listed with its run and removed through its host's git once confirmed, while an open run's and a merged run's are refused, the one rule it follows read branch by branch; the list, with a run's workflow worker down, showing each run in a moment, blocked by that worker, and reading it again once the worker is back — a closed run's end never kept as unknown; an answer or a change read while no worker can read the run refused at once, naming the worker, while a Stop and a force terminate are taken at once all the same; an answer or a removal whose run's status does not come in time — a worker dead too briefly for the preflight to count — refused, naming the worker, after one bounded read — controls: the read unbounded, and its failure not turned into a refusal — and Temporal's own query on a queue no worker polls ending at that bound as the same refusal — controls: another bound, and the failure not turned into a refusal; the operator's words, sent as the page sends a revise, reaching the engineer's next plan word for word — control: the server dropping them; a removal git refuses answered with git's word, run once and again only when asked, and one done already refused; a second removal while one runs refused; a removal refused at once, naming the worker, while no worker can read the run — control: its status never asked; each run's view — waiting at a stop since when with its actions, working at a stage and role since when, blocked by a host whose worker is down, closed once merged; a run started, listed, reviewed with its change read on its host, answered — an answer its stop does not offer refused by the workflow, one for a stop it has left refused, and one sent as the page sends it — the action the stop published, with no words written — refused by the workflow's own check — and merged; an answer to a run that has closed refused as not waiting, not as a failure of the page. These scenarios run in real time: on the time-skipping server, awaiting a workflow's result lets time jump to the parked run's default run timeout and close it |
| `agents/test_trust.py` | what a run records so its agents meet no trust dialog: the key each CLI on this host reads, the operator's own file kept whole (Claude's replaced atomically with its contents plus the entry, Codex's only appended to), nothing recorded twice however the path is spelled, a file we do not understand left alone, and a record that cannot be written costing a dialog rather than the run; the `prepare` activity records the run's repository for the brains it will use. That a record silences the real dialog is measured against the installed CLIs in acceptance, not here |
| `foundation/test_flows.py` | the flows a run may follow: both shipped flows hold and the policy's default names one; each rule refuses the flow that breaks it, saying which — a step's action and the role the stages give it, every plan and build followed by its review, an approval only after a review or research, research only before any plan — control: the rule taken out, a build only after a plan, a flow that builds ending at the merge, and from one to `MAX_FLOW_STEPS` steps — control: the upper bound taken out; a run handed its flow as `{name, steps}` and nothing else, never made into one — controls: the shape and the name unchecked; one grammar for a flow's name; a flow is its file in `flows/`, a broken or garbled one listed with its reason, one that cannot be read refused and listed so — control: the read error let through, and a name never reaches the flows that would hold beside the folder or in a folder of it — control: the name's guard taken out |
| `foundation/test_policy.py` | a role's persona file has one resolver: validation and the host running the role name the same file; a policy from inside the checkout crosses hosts as a name relative to it and is read against each host's own checkout — control: the checkout's own persona is another file; an origin only the other host can spell, and a missing persona, refused; nothing resolved on one host carried in the policy; a host's `ORCH_POLICY` read as its copy of the run's policy, and a copy that differs refused whether the prompt is relative or absolute; and, at the activity a target runs, a client's policy reaching the agent with this host's persona while an unreadable origin or a differing copy starts no agent — the Windows host suite is where the client's policy meets the other host; and no module assigning the path itself; the shipped policy's workflow queue kept as the one its earlier runs were started on |
| `interfaces/test_cli.py` | a start naming its flow with `--flow`, one that does not exist or breaks a rule refused with its reason before any call to Temporal, and a run ended `DONE` reported as a success; following a run — working at the first look, so waited on — leaves no call to Temporal in flight when it returns — control: a history long poll left open, or one inside the wait between looks, which the SDK answers after its caller stopped waiting; the command line types whatever actions a stop publishes — one it has no word for by its name, a revise's role as a word, guidance as it is written and only where a stop takes it, `yes`, and `--confirm` travelling with the answer — and lists the stop's own answers; `--stack` refused as a form of the run commands, never taken for a start — control: the refusal taken out |
| `test_architecture.py` | the rules this repository keeps about its own source: the direction its packages already depend in, read from `app/` so a new module needs no entry and a new package does, with nothing importing an entry point; that the checkout root `app/foundation/paths.py` derives still lands on a checkout and every other root path comes from it; that no unit test writes a trust record into the operator's real home — the one incident this suite has caused; that the project and every package walk the architecture address hop by hop, each router linking the level below it, and that the main view draws exactly the imports the table allows; and that a turn refuses an agent the host cannot wire, leaving nothing behind. Every check carries a control that proves it can reject a violation, and the boundary check was measured against a violation injected into the real tree |
| `workspace/test_repos.py` | the repository descriptor over detection: base branch, worktree root, todo convention, flags; only the done folder may be null; every refusal; a Windows target on a Linux path refused — control: a Windows process given one runs in `C:\Windows` |
| `test_harness.py` | a test process exits once its tests are done, even with a run's turn still at work on one of its workers — control: with the workers stopped only at `atexit`, after the interpreter has joined every thread pool's threads, it never exits; the parallel run: each class in a process of its own, a module split into its classes, a named module, class, single test or test file run alone and a repeated name once, and a failure, an error, a module that does not load — under its own name, saying its own error — a class that runs fewer tests than it holds, a run that finds or runs none, a flag or a name holding no tests, each failing it; a hung class dumped, then its processes ended; an interrupted run — interrupted again while it ends what runs — a terminated one, and one that fails any other way, leaving no class running; and the checkout never put on the PYTHONPATH its classes pass on — controls: each of those checks taken out |
| `__main__.py` · `runner.py` | the runner both scripts use, `python -m tests`: `python -m unittest` in one process, or with `--parallel` each class in a process of its own; either way, when the interpreter has not exited a minute after the tests, every thread's stack written out |
| `fakes.py`, `temporal_env.py` | the fake seams, and one test server with its workers per test process, stopped as the interpreter begins to shut down, before it joins the thread pools their activities run on; and a host of a test's own, whose worker the test can take away |
| `folders.py` | a test's temporary folder taken back whole — the repositories it made included, whose read-only objects Windows deletes only once they are writable |
| `fake_cli.py` | a stand-in for the interactive `claude` and `codex` CLIs: their completion hooks, Esc, typing and a detached descendant |
| `empty_workflow.py`, `control_workflows.py`, `control_trace_sink.py` | the controls: workflows that each lack one guarantee |
| `trace_rows.py` | a trace reduced to what the contract promises, for the row-by-row comparison |
| `temporal_cleanup.py` | deletes from Temporal the runs a check on the live stack made, the reads of their changes and the removals of what they kept, by exact id, and says what it still holds — `make demo`'s and the acceptance's cleanup |
| `record_histories.py` | records `histories/` — rerun only for a deliberate change to the workflow, behind `workflow.patched`; a new history by its name alone, so the ones older code wrote stay as they were |
| `acceptance_restart.py` | acceptance on the live stack with fake agents on a throwaway repository, on a stack of its own: each run on the acceptance's own workflow queue, new each time; the live stack stopped and started by the Workbench's own API from its service's context while a run waits — each worker's stop taking the stage settings left in that worker's own temporary folder, the Workbench reading the stack down while it is, a worker its side would start elevated refused, saying so, and started from the script's side instead, its service never restarted meanwhile; then the service restarted, and the WSL worker it started still running and polling in a scope of its own with the real CLIs on its PATH, and the installed unit read by systemd without a word; killed with SIGKILL, the service back by itself — a worker killed mid-role, run ids refused while open and after closing, a merge and a discard; that the run's own `prepare` recorded its repository for the CLIs it uses; that a plan changed at the approval stop is assessed again with no build turn run; and that the final gate's change — deliberately larger than one payload — comes in parts that all name one change and join byte for byte into the patch an independent `git` run prints. Afterwards it takes back all it made — its runs and the reads of their changes from Temporal, proven gone, their folders, its killed workers' pid files and its trust records — and fails on anything left |

Real Claude and Codex on real repositories are exercised by real runs, not by this suite.
